/* -------------------------------------------------------------------------
 *
 * Variables: Layout
 *
 * ------------------------------------------------------------------------- */

:root {
	--grid-margin: var(--wp--custom--spacing--grid-margin, clamp(round(down, 1rem, 4px), round(down, -0.3rem + 6.5vw, 4px), round(down, 7.5rem, 4px)));
	--grid-gutter: var(--wp--custom--spacing--grid-gutter, clamp(round(down, 1.5rem, 4px), round(down, 1.2rem + 1.5vw, 4px), round(down, 3rem, 4px)));
	--grid-content-width: var(--wp--style--global--content-size, 840px);
	--grid-wide-width: var(--wp--style--global--wide-size, 1136px);

	/* this should match the "Grid" size defined in the
	   `_experimentalLayout` setting within theme.json */
	--grid-max-width: var(--tribe--style--global--grid-size, 1680px);
}
